// tiny-kit — a small collection of pure string helpers.
//
// This barrel grows as each helper lands. Step 3 of the plan turns it into the
// real public surface by re-exporting `slugify` and `truncate`. Until then it
// just carries a version constant so the baseline check is green.
export const version = '0.0.0'
